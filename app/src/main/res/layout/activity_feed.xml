<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".FeedActivity">

    <!-- Spinner for Level Selection - Top Left -->
    <Spinner
        android:id="@+id/levelSpinner"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginTop="16dp"
        android:minHeight="48dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Logout Button - Top Right -->
    <Button
        android:id="@+id/logoutButtonTop"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:layout_marginEnd="16dp"
        android:text="Logout"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Your Existing Welcome TextView -->
    <TextView
        android:id="@+id/TextViewactivity_feed"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="Welcome!"
        android:textSize="20sp"
        app:layout_constraintTop_toBottomOf="@id/levelSpinner"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" /> <!-- Added horizontal constraints -->

    <!-- Game Grid View -->
    <com.example.travellog.gameui.GameGridView
        android:id="@+id/gameGridView"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="16dp"
        app:layout_constraintTop_toBottomOf="@id/TextViewactivity_feed"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintDimensionRatio="1:1" />


    <!--
        REMOVED THE OLD BUTTONS AND GUIDELINE:
        <TextView android:id="@+id/levelDisplayTextView" ... />
        <Button android:id="@+id/prevLevelButton" ... />
        <Button android:id="@+id/nextLevelButton" ... />
        <Button android:id="@+id/buttonlogout" ... />
        <androidx.constraintlayout.widget.Guideline android:id="@+id/guideline_vertical_center" ... />
    -->

</androidx.constraintlayout.widget.ConstraintLayout>
